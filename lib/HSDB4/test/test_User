#!/usr/bin/perl -w

use strict;
use HSDB4::test::setup;
use HSDB4::SQLRow::User;

my $user = HSDB4::SQLRow::User->new ()->lookup_key('salbrigh');
my @groups = $user->parent_user_groups;
for ($user->current_evals) {
    print $_->out_label, "\n";
}
1;
