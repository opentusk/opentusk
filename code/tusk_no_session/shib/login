<%doc>
	This page takes the ENV passed in by shibboleth authentication
	It then calls TicketMasterShib methods to create the session
	Finally it rerturns success or a redirect to the login page
</%doc>

<%once>
use Apache::TicketMasterShib;
</%once>

<%perl>
$m->clear_buffer();
#$r->content_type('text/html');
#$m->print("<html><head><title>Here</title></head><body>Made it here</body></html>");
return Apache::TicketMasterShib::authenticate(\%ENV, $r);
</%perl>
