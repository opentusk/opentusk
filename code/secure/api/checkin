[- 
	use HSDB45::API;
	$document = HSDB45::API->new('document_save');
	$data = $fdat{'data'};
	#while (<$filehandle>) {
    	#	$data .= $_;
	#}
 	$document->document_save($fdat{'token'},$fdat{'content_id'},$data,$fdat{'modified_note'},$fdat{'status'},$fdat{'status_note'}); 
	$output = $document->output_xml;
	$http_headers_out{'Content-Type'} = $document->content_type;
-]
[+ 	$output +]
