[-
	use HSDB45::API;
 	$login = HSDB45::API->new('login');
 	$login->login($req_rec,lc($fdat{'username'}),$fdat{'password'});
	$output = $login->output_xml;
	$http_headers_out{'Content-Type'} = $login->content_type;
-]
[+ 	$output +]
