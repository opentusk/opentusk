[- 
	use HSDB45::API;
	$document = HSDB45::API->new('document_load');
 	$document->document_load($fdat{'token'},$fdat{'content_id'}); 
	$output = $document->output_xml;
	$http_headers_out{'Content-Type'} = $document->content_type;
-]
[+ 	$output +]
