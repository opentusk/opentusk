[- 
	use HSDB45::API;
	$document = HSDB45::API->new('course_choose');
 	$document->course_choose($fdat{'token'}); 
	$output = $document->output_xml;
	$http_headers_out{'Content-Type'} = $document->content_type;
-]
[+ 	$output +]
