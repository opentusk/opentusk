[-
	use HSDB45::API;
 	$document = HSDB45::API->new('umls_choose');
 	$document->umls_choose($fdat{'token'},$fdat{'word'});
	$output = $document->output_xml;
	$http_headers_out{'Content-Type'} = $document->content_type;
-]
[+ 	$output +]

