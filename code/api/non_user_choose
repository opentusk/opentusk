[-
	use HSDB45::API;
 	$document = HSDB45::API->new('non_user_choose');
 	$document->non_user_choose($fdat{'token'},$fdat{'name'});
	$output = $document->output_xml;
	$http_headers_out{'Content-Type'} = $document->content_type;
-]
[+ 	$output +]

