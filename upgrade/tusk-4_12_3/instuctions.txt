## Add a constrain to the course_user table and remove existing duplicates:

mysql -h [dbhostname] -u [dbuserid] -p < course_user.sql

## Use HTTPS for the home page:

cp tusk_common tusk_http.conf tusk_https.conf /usr/local/tusk/conf/etc/httpd/conf.d
ln -s /usr/local/tusk/conf/etc/httpd/conf.d/tusk_common /etc/httpd/conf.d

# Replace TUSKFQDN with the server name in tusk_http.conf and tusk_https.conf.
