To update the database, run the following commands:

Add indices to link_course_students
> ./update_indices.pl

Add published flag to eval
> ./update_published.pl

Optional:
Add the virtual role 'site_eval_admin' to 'tusk.permission_role'
